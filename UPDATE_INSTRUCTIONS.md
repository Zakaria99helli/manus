# 🚀 تعليمات تحديث مشروع Damas Food

## 📋 ملخص التحديثات

### ✅ ما تم إنجازه:

#### 1. **تحديث Schema (قاعدة البيانات)**
- ✅ إضافة جدول `tables` (الطاولات)
- ✅ إضافة جدول `extras` (الإضافات العامة)
- ✅ إضافة جدول `categories` (الفئات)
- ✅ تحديث `menuItems` مع دعم:
  - أسعار متعددة (عادي/مع بطاطا/مع كولا/وجبة)
  - أسماء بثلاث لغات (فرنسي/عربي/إنجليزي)
  - وصف ومكونات
- ✅ إضافة جدول `menuItemExtras` (ربط الأصناف بالإضافات)
- ✅ تحديث `orders` مع دعم حالة الطلب
- ✅ إضافة جدول `orderItems` (تفاصيل الطلب)

#### 2. **تحديث Backend API**
- ✅ Routes كاملة لـ:
  - إدارة الطاولات (CRUD + QR Code)
  - إدارة الإضافات (CRUD)
  - إدارة الفئات (CRUD)
  - إدارة المنيو (CRUD + رفع صور)
  - إدارة الطلبات (مع حالة الطلب)
  - إدارة المستخدمين

#### 3. **Seed Script**
- ✅ بيانات افتراضية:
  - 5 فئات (سندويشات، صحون، مشروبات، مقبلات، حلويات)
  - 23 إضافة (صوصات، خضار، إضافات، مشروبات)
  - 10 طاولات مع QR Codes
  - مستخدم admin افتراضي

---

## 🔧 خطوات التطبيق

### 1. **رفع التحديثات على GitHub**

```bash
# داخل مجلد المشروع المحدث
git add .
git commit -m "feat: Add tables, extras, multi-language support and improved admin panel"
git push origin main
```

### 2. **تحديث قاعدة البيانات على Render**

بعد ما ينشر المشروع تلقائياً على Render:

1. روح على **Render Dashboard**
2. افتح **Web Service** تبعك
3. اضغط **Shell** (من القائمة الجانبية)
4. شغّل الأوامر التالية:

```bash
# تحديث Schema
npm run db:push

# إضافة البيانات الافتراضية
npm run db:seed
```

### 3. **التحقق من النجاح**

افتح الموقع وسجل دخول بـ:
- **Username:** `admin`
- **Password:** `admin123`

---

## 📁 الملفات المحدثة

### Backend:
- `shared/schema.ts` - Schema الجديد
- `server/routes.ts` - API Routes المحدثة
- `server/seed.ts` - Script البيانات الافتراضية
- `package.json` - إضافة `db:seed` script

### Frontend (قيد التطوير):
- صفحات Admin محسّنة
- صفحة الزبون مع الإضافات
- صفحة الكاشير
- نظام اللغات

---

## ⚠️ ملاحظات مهمة

### 1. **Build Command على Render**
تأكد إنو Build Command هو:
```bash
npm install --include=dev && npm run build && npm run db:push
```

### 2. **Environment Variables**
تأكد من وجود:
- `DATABASE_URL` - رابط قاعدة البيانات
- `NODE_ENV=production`
- `PORT` (اختياري)

### 3. **أول مرة تشغيل**
بعد أول deploy، **لازم تشغل** `npm run db:seed` من Shell عشان تضيف البيانات الافتراضية!

---

## 🎯 الخطوات التالية (Frontend)

### ما زال قيد التطوير:

1. **صفحة Admin Panel:**
   - إدارة الطاولات (عرض/إضافة/حذف/نسخ رابط QR)
   - إدارة الإضافات (عرض/إضافة/تعديل/حذف)
   - إدارة المنيو (رفع صور/تعديل أسعار/إضافة إضافات)
   - إدارة الفئات

2. **صفحة الزبون:**
   - عرض المنيو حسب الفئة
   - اختيار الصنف مع الإضافات
   - السلة الكاملة
   - إرسال الطلب مع رقم الطاولة

3. **صفحة الكاشير:**
   - عرض الطلبات الحية
   - تغيير حالة الطلب (قيد التحضير/جاهز/تم التوصيل)
   - عرض رقم الطاولة والتفاصيل

4. **نظام اللغات:**
   - دعم الفرنسي (الأساسي)
   - دعم العربي
   - دعم الإنجليزي
   - زر تبديل اللغة

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من Logs على Render
2. تأكد من تشغيل `db:push` و `db:seed`
3. تحقق من Environment Variables

---

**تم التحديث:** $(date)
**الإصدار:** 2.0.0-beta
