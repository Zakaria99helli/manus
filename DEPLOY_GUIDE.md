# 🚀 دليل رفع المشروع على GitHub و Render

## الجزء الأول: رفع المشروع على GitHub

### الخطوة 1️⃣: تهيئة Git في المشروع

افتح Terminal (موجه الأوامر) داخل مجلد المشروع وشغّل:

```bash
git init
```

---

### الخطوة 2️⃣: إضافة جميع الملفات

```bash
git add .
```

---

### الخطوة 3️⃣: عمل Commit (حفظ التغييرات)

```bash
git commit -m "Fixed project structure and build errors"
```

---

### الخطوة 4️⃣: تغيير اسم الفرع لـ main

```bash
git branch -M main
```

---

### الخطوة 5️⃣: ربط المشروع مع GitHub

```bash
git remote add origin https://github.com/Zakaria99helli/manus.git
```

---

### الخطوة 6️⃣: رفع المشروع

```bash
git push -u origin main
```

**ملاحظة:** إذا طلب منك اسم المستخدم وكلمة المرور:
- اسم المستخدم: `Zakaria99helli`
- كلمة المرور: استخدم **Personal Access Token** (مش كلمة المرور العادية)

#### كيف تحصل على Personal Access Token:
1. اذهب إلى: https://github.com/settings/tokens
2. اضغط **Generate new token** → **Generate new token (classic)**
3. اختار الصلاحيات: `repo` (كل الصلاحيات تحت repo)
4. اضغط **Generate token**
5. **انسخ التوكن** (ما رح يظهر مرة تانية!)
6. استخدمه بدل كلمة المرور

---

## الجزء الثاني: النشر على Render

### الخطوة 1️⃣: إنشاء حساب على Render

اذهب إلى: https://render.com وسجل دخول (يفضل بحساب GitHub)

---

### الخطوة 2️⃣: إنشاء قاعدة بيانات PostgreSQL

1. من لوحة التحكم، اضغط **New +**
2. اختار **PostgreSQL**
3. املأ البيانات:
   - **Name:** `restaurant-db` (أو أي اسم تريده)
   - **Database:** `restaurant_db`
   - **User:** (سيتم إنشاؤه تلقائياً)
   - **Region:** اختار الأقرب لك
   - **Plan:** **Free** (مجاني)
4. اضغط **Create Database**
5. **انتظر** حتى تصبح الحالة "Available"
6. **انسخ** الرابط من **Internal Database URL** أو **External Database URL**

---

### الخطوة 3️⃣: إنشاء Web Service

1. من لوحة التحكم، اضغط **New +**
2. اختار **Web Service**
3. اضغط **Connect GitHub** (إذا لم تكن قد ربطت حسابك)
4. اختار المستودع: `Zakaria99helli/manus`
5. املأ الإعدادات:

#### إعدادات أساسية:
- **Name:** `damas-food-restaurant` (أو أي اسم)
- **Region:** نفس المنطقة اللي اخترتها لقاعدة البيانات
- **Branch:** `main`
- **Root Directory:** (اتركه فارغ)

#### إعدادات البناء:
- **Build Command:**
  ```bash
  npm install && npm run build
  ```

- **Start Command:**
  ```bash
  npm start
  ```

#### متغيرات البيئة (Environment Variables):
اضغط **Add Environment Variable** وأضف:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `DATABASE_URL` | (الصق رابط قاعدة البيانات من الخطوة 2) |

#### الخطة:
- **Instance Type:** **Free** (مجاني)

6. اضغط **Create Web Service**

---

### الخطوة 4️⃣: إنشاء جداول قاعدة البيانات

بعد ما ينتهي النشر بنجاح:

1. اذهب إلى صفحة الـ Web Service
2. اضغط على تبويب **Shell** (في الأعلى)
3. شغّل الأمر:
   ```bash
   npm run db:push
   ```

هذا سيُنشئ الجداول في قاعدة البيانات.

---

### الخطوة 5️⃣: فتح الموقع

بعد اكتمال النشر:
1. اضغط على الرابط في أعلى الصفحة (مثل: `https://damas-food-restaurant.onrender.com`)
2. **تهانينا! موقعك شغال على الإنترنت! 🎉**

---

## 🔄 تحديث المشروع لاحقاً

عندما تعمل تعديلات على المشروع:

```bash
# حفظ التعديلات
git add .
git commit -m "وصف التعديل"

# رفع على GitHub
git push origin main
```

**Render سيكتشف التحديث تلقائياً ويعيد النشر!** 🚀

---

## ⚠️ ملاحظات مهمة

### الخطة المجانية على Render:
- ✅ مجانية 100%
- ⚠️ السيرفر ينام بعد 15 دقيقة من عدم الاستخدام
- ⏱️ أول طلب بعد النوم يأخذ 30-60 ثانية
- 💾 قاعدة البيانات المجانية: 1GB تخزين

### إذا واجهت مشاكل:
1. تحقق من **Logs** في Render
2. تأكد من `DATABASE_URL` صحيح
3. تأكد من تشغيل `npm run db:push`

---

## 📞 الدعم

إذا واجهت أي مشكلة، تواصل معي أو افتح issue على GitHub!

**بالتوفيق! 🎯**
